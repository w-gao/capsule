{
  "name": "capsule",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "license": "MIT",
  "scripts": {
    "dev": "lerna run dev --stream --parallel",
    "build": "lerna run build --stream",
    "prestart": "yarn",
    "start": "cd packages/server && yarn start"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "typescript": "^4.6.3"
  }
}
